import { NgxAutocompletePipe } from './ngx-autocomplete.pipe';

describe('NgxAutocompletePipe', () => {
  it('create an instance', () => {
    const pipe = new NgxAutocompletePipe();
    expect(pipe).toBeTruthy();
  });
});
